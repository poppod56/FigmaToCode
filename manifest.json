{
  "name": "FigmaToCode",
  "id": "1674341622595555113",
  "api": "1.0.0",
  "documentAccess": "dynamic-page",
  "main": "code.js",
  "ui": "ui.html",
  "editorType": ["figma"],
  "networkAccess": {
    "allowedDomains": ["http://localhost:8787", "ws://localhost:8788"],
    "devAllowedDomains": ["*"],
    "reasoning": "Optional local servers on the user's own machine, used only while the matching Connect button in Settings is toggled on: localhost:8787 dumps the selected layers for troubleshooting, localhost:8788 is a WebSocket bridge to a local MCP server so an AI client can read the current selection. Nothing is sent anywhere by default and no data leaves the user's own machine."
  }
}
